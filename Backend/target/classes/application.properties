
# MongoDB Configuration
spring.data.mongodb.uri=""

spring.data.mongodb.auto-index-creation=true

# JWT Secret Key (Use a secure, long, and random value)
jwt.secret=" "

# Access Token Expiration Time (e.g., 1 day in milliseconds)
jwt.expiration=86400000  

# Refresh Token Expiration Time (e.g., 7 days in milliseconds for longer session persistence)
jwt.refresh-expiration=604800000  

jwt.refreshExpirationMs=604800000

logging.level.org.springframework.data.mongodb=DEBUG

# Email Service Configuration (Using environment variables for security)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=" "
spring.mail.password=" "
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

cors.allowed-origins=http://localhost:5173,http://localhost:8081,http://localhost:8080
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type,X-Requested-With,Accept,Origin,Access-Control-Request-Method,Access-Control-Request-Headers

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/oauth2/jwks

logging.level.org.springframework=DEBUG
logging.level.com.projectstack.api=DEBUG


# Server Configuration
server.port=8080
